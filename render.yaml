services:
  - type: web
    name: inkinsights-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python upload.py
    plan: free
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /
    autoDeploy: true
